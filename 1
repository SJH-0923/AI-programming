from tkinter import *
from tkinter import messagebox
import json
import random as rd


try:
    with open("memo.json", "r", encoding="utf-8") as f:
        memo_data = json.load(f)
except (FileNotFoundError, json.JSONDecodeError):
    memo = {}

with open("food.json", "r", encoding="utf=8") as f:
    food_data = json.load(f)

def enter_memo() :
    memo()

def random_food() :
    a = rd.choice(food_data)
    return a

def memo() :
    memo_window = Toplevel(window)
    memo_window.title("메모장")
    memo_window.geometry("300x300")

def todo() :
    pass


def calender() :
    pass


def food() :
    food_label2.config(text = f"<{random_food()}>")



window = Tk()
window.title("대학생 올인원")
window.geometry("450x450")

#타이틀
title = Label(window, text = "대학생 올인원 앱", font = ("맑은 고딕", 30), fg = "blue")
title.pack(side = TOP)

#라벨
food_label1 = Label(window, text = "오늘의 메뉴 추천", font = ("맑은 고딕", 30))
food_label2 = Label(window, font = ("맑은 고딕", 30))
food_label1.pack()
food_label2.pack()

#사진
photo_memo_original = PhotoImage(file = "all_in_one_pic/memo.png")
photo_memo = photo_memo_original.subsample(5, 5)

#버튼
button_food = Button(window, text = "!돌리기!", fg = "blue", command = food)
button_memo = Button(window, image = photo_memo, command = enter_memo)
button_quit = Button(window, text = "앱 종료", fg = "red", command = quit)

button_food.pack(pady=50)
button_memo.pack(pady=50)
button_quit.pack(pady=50)

window.mainloop()
