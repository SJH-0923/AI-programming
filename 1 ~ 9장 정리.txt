unix

쉘의 차이

python

Ubuntu 깔아라

C:\Users\me>c:
C:\Users\me>cd\
C:\mkdir workspace
C:\>cd workspace
C:\workspace>

----------------------------------------------------------------------------------------------------------
<1장>
대화식 실행 모드 : 그 자리에서 바로 결과 보여줌 -> 간단한 코드 테스트
스크립트 파일 실행 모드 : .py로 파일을 만들어서 복잡한 로직이 있는 코드 만들어서 실행

-----------------------------------------------------------------------------------------------------------
<2장>
-변수 : 변할 수 있는 수, 메모리 위치의 이름
-식별자 : 서로 구별되는 이름 부여
	문자, 숫자, 언더바로 이루어짐.
	공백 있으면 안됨.
	첫 글자는 문자나 언더바
	대문자와 소문자는 구별됨. C 와 c는 다른 식별자
	식별자 길이 제한 없음
	키워드는 식별자로 사용 안됨
-메인 메모리 : 읽기, 쓰기, 덮기
-메모리 주소 : 보통 16진수로 표현, 데이터가 저장된 곳
-비트 : 정보 표현의 최소의 단위, 0과 1을 사용
-바이트 : 8비트 단위, 2의 8승 즉 256가지 서로 다른 상태 정보 표현
-리터럴 : 프로그래밍 언어에서 데이터 값을 나타냄
-키워드 : 이미 예약된 문자, 미리 지정된 역할 수행
	import, for, if, def, class, False, None, True 등등...
-연산자 : 산술 연산, 연산자는 문자열 사용 불가, 특정한 자료형에서만 사용 가능
	+, -, *, /, **, %, // : 덧셈, 뺄셈, 곱셈, 나눗셈, 제곱, 나머지, 몫
-자료형 : 부울형, 숫자형, 문자열, 리스트, 튜플, 집합, 딕셔너리
-type : 자료형 확인 방법 : type(a)
-동적형 결정 : num 이라는 변수에 정수 값이 할당되면 변수의 자료형이 int로 결정됨. 프로그램 동작이 유연함. 
-정적형 결정 : 프로그램이 실행되기 전에 미리 결정되는 것을 의미
-str : 문자열 객체로 만들어서 반환하는 함수 : str(100) / x = [a, b, c] str(x)
	작은따옴표나 큰따옴표로도 사용 가능
	문자열 안에 따옴표 넣을 경우 : "나는 \"안녕?\" 하고 인사했다."
-수치 자료형 : int, float, bool(참과 거짓 의미), string
-복소수 자료형(complex) : 허수 부분을 'j'로 표현해야 함 : c1 = 2+3j
	c1.real = 2 / c1.imag = 3
-켤레 복소수 : conjugate() : c2 = c1.conjugate()
	크기를 알려면 : abs(c1)
-할당 : 좌변에 변수, 우변에 값
-복합 할당 연산자 : +=, -=, *=, /=, //=, %=, **=
-비교 연산자 : ==,  !=, >, <, >=, <=
-논리 연산자 : and, or, not
	0, None, 빈 리스트 등 내용이 비었으면 bool 출력
-비트 연산자 : &, |, ^, ~, <<, >> / and, or, xor, not, 비트 단위 외쪽으로 이동, 비트 단위 오른쪽으로 이동 / 마찬가지로 복합 할당 연산자 ~ 빼고 다 가능
-input() 문 : 이 함수는 str형으로 값을 받아들인다.

---------------------------------------------------------------------------------------------------------
<3장>
-순차문 : 위부터 아래로 순차적인 흐름으로 코드가 실행되는 구조
-제어문 : 프로그램의 흐름을 제어하는 역할
-조건문 : if, if-else 문
-반복문 : for 문, while 문
	반복문 흐름 변경 : break, continue
-블록(block) : 어떤 조건을 만족하는 경우에 특정한 코드를 선택적으로 실행
-복합 조건식 : 더 정교한 조건을 걸어주기 위해 조건 연산자와 논리 연산자를 조합
-논리 연산 : and, or
-for : 반복의 횟수가 미리 정해져 있는 경우 : for i in range(n) : ---> i는 (루프 제어)변수, n은 반복 실행 횟수
-while : 반복하는 조건이 명확한 경우
-range : range([start], [stop], [step]) 순서. : range(0, 5, 1)이면 (인덱싱 기준)0 ~ 5까지 1씩 더하면서 진행
-줄바꿈 기호 \n : 기본적으로 print()는 print( , end = '\n')가 있는데 end = '\n'는 생략이 되어 있다.
			그래서 가로로 배열하고자 할 때는 end = ' '로 \n을 지워주면 됨
-리스트에서 for 문 : a = [1,2,3,4,5] / for i in range(a) : ---> 첫 번째 요소부터 차례대로 받아옴
-문자열 자료형은 list() 함수를 이용해서 각 단어를 리스트 객체로 만드는 것이 가능 : a = "hello", list(a) ---> 'h', 'e', 'l', 'l', 'o'
-n = 5, for i in range(n) 이라고 하면, n은 0, 1, 2, 3, 4를 받아옴 (인덱싱이 기준임)
-while문 : while 조건식 :
		실행할 코드 ---> 조건식이 참이라면 계속 반복
-break : 반복 실행을 종료
-continue : 반복 루프 내의 나머지 실행부를 건너뛰고 해당 반복문의 처음으로 돌아가서 계속해서 해당 반복 루프를 실행

----------------------------------------------------------------------------------------------------------
<4장>
-내장함수 : 파이썬에서 미리 만들어진 함수 : print()
-사용자 정의 함수 : 사용자가 직접 필요한 함수를 만들 수 있음 : def func_name(a,b,c, ...) :
												code1
												code2
												return .....
-반환문 return : 결과를 반환해 주는 기능








ls : 리스트 보여줌
cd : change direction 경로 설정
cd .. : 설정한 경로 나가기
pwd : Print Working Directory 작업하고 있는 폴더의 경로를 출력
mkdir : makd directory 디렉토리를 만듦
rmdir : remove directory 디렉토리 지움
echo : 문자열이나 객체를 화면에 출력해줌
ls -al : 숨겨진 파일까지 다 보여줌
rm : 파일 싹 다 삭제

깃허브에 처음 올리는 과정
git init
echo "# 2026_AI_Program" >> README.md
git add README.md
git commit -m "2026.03.18 coding"
git branch -M main
git remote add origin https://github.com/SJH-0923/2026_AI_Program.git
git push -u origin main


깃허브에 올리는 순서
git add . : 폴더 안에 있는 모든 파일들을 담는다.
git add . 파일명: 파일 하나만 담는다.
git commit -m "메시지" : commit을 해야 포장이 된 거임. 따옴표 안에 글이 무조건 있어야 함.
git push origin main

git push origin main : 깃허브에 올리기
git pull origin main : 깃허브에서 불러오기

결과가 없는 함수 : print
결과가 있는 함수 : input



<제어문>
조건문 : if 문, if - else 문
반복문 : for 문, while 문
반복문 흐름 변경 : break 문, continue

-조건문
if : 조건 적용
else if (elif) : else 중에서 추가적인 조건 적용
else : if, elif 말고 다

-반복문
for i in range() : range 안에서 i를 받아와라.
range()에서 괄호 안의 순서 (start, stop, step) 시작, 멈춤, 간격
※ i는 단순 매개변수라서 다른 기호를 사용해도 상관 없다.

-리스트 변수
score = [87,84, 95, 67]
for i in score(리스트 변수)를 넣으면 리스트를 프린트 해준다. 숫자 및 문자로도 받을 수 있다.
list(range(5)) : range(5)를 list로 바꾼다.

myString = "apple"
for ch in myString :
    print(ch) ---> 이렇게 입력하면 문자 스펠링 하나하나가 따로따로 찍힌다.

리스트가 -1이면 리스트의 제일 마지막 요소를 프린트 함. (한 바퀴만 돌 수 있다.)

list.append('') : list라는 리스트 안에 새로운 요소를 추가한다.

!!! 리스트 매서드 !!!
메소드 : 객체가 가지고 있는 명령어
list.메소드 : 리스트는 정보도 담고 있지만 행동(추가, 삭제 등)이 가능 ex) .append() 메소드 등
.append() : 요소 추가
.pop() : 요소 삭제 (괄호 안에 숫자 있으면 순서, 없으면 맨 마지막 요소 삭제)
.remove : 요소 삭제(괄호 안에 요소를 넣어서 삭제)


min() : 
max() : 
sum() : 
reverse() : sort reverse = True 랑 똑같음

list[1:5] : 1요소부터 5요소까지 슬라이싱. (앞에서부터 start, stop, step 순으로 똑같다.)

줄바꿈 기호 \n
기본적으로 print()는 print( , end = '\n')가 있는데 end = '\n'는 생략이 되어 있다.
그래서 가로로 배열하고자 할 때는 end = ' '로 \n을 지워주면 됨

dictionary
key와 value가 존재
형태 : 딕셔너리 이름 = {key : value, ...}
person = {'이름' : '홍길동', '나이' : 26, '몸무게' : 82 }
위와 같이 했을 때, 이름(key)과 홍길동(value)가 있다
print(person[key값])을 하면 해당 value를 준다

dictionary에 요소 추가 방법
형태 : 딕셔너리 이름[key] = value
person['국적'] = '대한민국'

dictionary에 요소 삭제 방법
형태 : del 딕셔너리 이름['key']
del person['국적']

딕셔너리 부등호 지원 안 함.

!!! 딕셔너리 메서드 !!!

(파이썬 모든 변수는 객체변수)

튜플
튜플과 리스트의 차이 : 튜플은 정의하면 추가 및 삭제가 안 된다.
함수의 아웃풋은 튜플
output_list = list(output) 아웃풋 값을 리스트로 바꾸는 방법

튜플의 +, * 연산도 리스트와 같다.
ex) 같은 튜플을 더하면 값이 더해지는 게 아니라 같은 튜플이 두 개

!!! 튜플 매서드 !!!

집합
집합명 = {요소들} ---> 집합은 중괄호 사용

!!! 집합 매서드 !!!

json 파일
import json
json.dump : 파일을 저장 및 생성
json.dumps : 파일을 생성하지는 않고 문자열로 변환하여 변수에 담음.

기존에 만들었던 주소록을 유지하고 거기서 추가추가 하는 방식의 코딩을 짜라.
그거 말고도 주소록 저장을 위한 편리한 기능 넣어서 코딩을 짜라.
기존 주소록을 삭제하는 기능
주소록을 탐색하는 기능 등등...

모듈
다른 사람이 만들어 놓은 거 쓰는 거
매서드는 class에 속한 함수
[모듈 이름].[클래스 이름].[메소드이름]() ---> datetime.datetime.now.()
import ~ as 별칭

dir 함수 : 모듈이 가진 클래스의 목록을 출력
replace() 메소드 : datetime에서 날짜나 시간 값을 변경하고 싶을 때 사용

print의 아웃풋 = str(문자열)

class변수의 특징
메서드(함수) 가능

클래스 > 인스턴스

클래스 내에서 함수를 쓸 때 self라는 변수를 사용

class 뭐시기 :
	def __init__(self, .....)
이 형식을 외워라

문자열화 메소드
__init__ : self 를 초기화 하는 코드
__str__ : 문자열을 부르고 문자열을 프린트. 반드시 문자열을 return 해야 함.
.__name : 이것처럼 변수 앞에 언더바 2개를 붙이면 캡슐화(암호화) 하는 거임.

캡슐화까지 시험범위.

!!! 특수 메서트 !!!
클래스 안에서 __함수 이름__ : 이렇게 지으면 특수 함수가 됨. ---> 연산기호(+)로 가능하게 함.

정적 변수

클래스의 상속

위에서 Car을 다 짜 놓았으면 class Sedan(Car) 이렇게 하면 Sedan이라는 클래스도 Car의 속성 그대로 받아 온다.
하지만 class Sedan(Car) 밑에 똑같은 이름의 함수를 재정의 했으면, 그 함수는 재정의 한 것으로 됨.

윈도우 기준으로 할 것인가
박스 기준으로 할 것인가

GUI  코딩에 무조건 필요한 코드 : 이거를 쓰면 tk라는 창이 하나 뜬다.

from tkinter import*
window = Tk()
window.mainloop()

Tk도 클래스의 일종이다.

Label() : 문자를 표현할 수 있는 위젯
Photoimage() : 레이블 대신에 이미지를 넣을 수 있는 코드

!!!마우스 및 키보드 event 코드!!!